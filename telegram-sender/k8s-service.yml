apiVersion: v1
kind: Service
metadata:
  name: telegram-sender-service
spec:
  type: ClusterIP
  selector:
    app: telegram-sender-pod-lbl

  ports:
    - name: http
      port: 8080
      targetPort: 8080