apiVersion: v1
kind: Service
metadata:
  name: telegram-agent-service
spec:
  type: ClusterIP
  selector:
    app: telegram-agent-pod-lbl

  ports:
    - name: http
      port: 8080
      targetPort: 8080